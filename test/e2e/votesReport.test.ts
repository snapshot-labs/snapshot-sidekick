describe('GET /api/votes/:id', () => {
  describe('when proposal exists and cached', () => {
    it.todo('returns the cached file');
  });

  describe('when proposal exists and not cached', () => {
    it.todo('returns a 202 status code');
  });

  describe('when proposal does not exist', () => {
    it.todo('returns a 404 error');
  });
});
